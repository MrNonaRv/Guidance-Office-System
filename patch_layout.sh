#!/bin/bash

# Target range to replace is approximately line 1918 to 2043 (which contains the grid layouts)
# We will use awk to replace the exact block. Let's first dump the file and use a simpler script to patch.

